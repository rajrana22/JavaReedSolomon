apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'com.backblaze.erasure.ReedSolomonBenchmarkMLEC'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation group: 'junit', name: 'junit', version: '4.+'
}

jar {
    archiveBaseName = 'gs-gradle'
    archiveVersion =  '0.1.0'
}